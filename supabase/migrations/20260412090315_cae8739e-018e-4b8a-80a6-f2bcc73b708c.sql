ALTER TABLE public.jobs DROP CONSTRAINT jobs_status_check;
ALTER TABLE public.jobs ADD CONSTRAINT jobs_status_check CHECK (status = ANY (ARRAY['scheduled','in_progress','complete','flagged','pending','confirmed','awaiting_quote','cancelled']));